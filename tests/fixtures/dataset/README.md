# Dataset validation fixture

This anonymized fixture tests contracts and directory validation. Its `video.mp4` is deliberately
plain-text placeholder content and `metadata.json` declares `test_fixture: true`; it must never be
treated as valid media or scientific evidence. The pressure CSV demonstrates mixed calibrated and
uncalibrated cells on one arbitrary multi-cell mat.
